import { ErrorStyled } from './Error.styled';

export const Error = () => (
  <ErrorStyled>
    Could not load the video. Either it doesn't exist, or an error has occurred
    while loading.
  </ErrorStyled>
);
