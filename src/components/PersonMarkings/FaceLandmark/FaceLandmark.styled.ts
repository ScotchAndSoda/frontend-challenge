import styled from 'styled-components';

export const FaceLandmarkStyled = styled.div`
  position: absolute;
  background-color: ${({ theme }) => theme.colors.tertiary};
  height: 4px;
  width: 4px;
`;
